name: setup-konjure
description: Setup Konjure on GitHub Actions runners
inputs:
  version:
    description: Version of Konjure to install
    required: false
    default: latest
runs:
  using: node24
  main: dist/index.js
